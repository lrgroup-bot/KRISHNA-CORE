# KRISHNA Pre-Pull Historical Branch Reconciliation — 2026-09-23

This audit was performed before updating the owner PC from the canonical branch.

## Authority

Canonical branch: `fix/krishna-ui-runtime-verification`

Historical and divergent feature/integration branches are evidence for comparison only. They are not runtime authority and must never override newer source merely because a commit is unique.

## Reconciliation results

### Restored into current architecture

- **Rishi Vishvakarma** — permanent design/UI/software-craft knowledge curator with explicit provenance and a clear boundary between modern KRISHNA design roles and historical/traditional claims.
- **Vishvakarma curriculum + learning store** — candidate/verified knowledge lifecycle with source, version, license and evidence.
- **Sudarshan Design Engine** — selective skill routing, design genome, recursive design-drift comparison and hard acceptance gates.
- **Sudarshan UI repair loop** — bounded repair/retest/escalate lifecycle.
- **Local design-tool adapters** — Playwright CLI discovery, Storybook component-state contract and opt-in Stagehand recovery.
- **Local Model Scout** — persistent local candidate scoring/recommendation by quality, latency and RAM/VRAM fit; no automatic download or routing authority.
- **Structured Project Brain** — restored PROJECT/ARCHITECTURE/RULES/PHASES/DESIGN/AGENTS/MEMORY governance layers while preserving the newer DB-backed verified-learning/context API. Governance state is runtime-owned by default so deployment does not dirty source projects.
- **Architecture truth CLI/CI gate** — wrapper around the current canonical `ArchitectureTruthAudit`, failing on missing indexed evidence or invalid product-truth statuses.

### Already present / newer current implementation

- HAWKEYE unified coordinator, references, diagnostics and mobile evidence pipeline.
- BHOOMIPUTRA field perception and survey engine.
- Canonical mobile_v3, wake service, encrypted edge evidence, background sync and evidence curator.
- Shared Action migration for priority owner mutations.
- Sudarshan project lifecycle and mandatory design policy.
- KABACH security/privacy runtime.
- Zero-cost OpenRouter fabric and verified-direct-free Cloudflare boundary.

### Intentionally superseded — not restored

- `cloud_providers.py` and `unified_model_mesh.py` from the old integration branch. They trusted a provider-side `confirmed_free` declaration and are weaker than the current hierarchy:
  **local -> live-zero OpenRouter -> live-zero-billing verified direct provider -> local fallback/STOP**.
- Old `diagnostic_engines.py`. The current HAWKEYE diagnostic runtime + structured adapters + read-only evidence transport are safer and more complete.
- Old standalone architecture-audit implementation. The current `architecture_truth.py` remains canonical; only a CLI wrapper was restored.

## Verification rule

Unique historical commits are not proof of missing functionality. A historical feature is restored only when:
1. its capability is absent from the canonical source,
2. no newer implementation supersedes it,
3. it fits the current KRISHNA/Sudarshan authority model,
4. it receives tests and product-truth evidence,
5. CI passes after reconciliation.

## Pull gate

Do not pull/deploy to the owner PC until the reconciliation PR passes Windows + Ubuntu Core, browser/privacy E2E, spatial UI build, architecture truth, and Windows build/smoke/restart gates.
